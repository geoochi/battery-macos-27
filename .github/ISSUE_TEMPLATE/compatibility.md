---
name: Compatibility or bug report
about: Report a model/build result without sharing personal logs
---

## Environment

- Model identifier (`sysctl -n hw.model`; NOT serial number):
- macOS version and build (`sw_vers`):
- CLI version (`battctl --version`):
- Adapter connected / lid open or closed / awake or sleeping:

## Command and outcome

- Command:
- Expected:
- Observed:
- Did the command only read status, stage a preference, or verify after restart?

## Redacted evidence

Paste only relevant fields. Remove exact timestamps, user paths and process details.
Do not attach full system logs, IORegistry dumps, recorder files or credentials.
If the model/build is refused, report the refusal; do not remove the write guard.
